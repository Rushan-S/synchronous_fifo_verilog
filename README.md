# synchronous_fifo_verilog
Synchronous FIFO design in Verilog with full testbench verification. Implements read/write pointers, occupancy counter, and status flags (empty, full, almost_full, almost_empty). Verified using ModelSim.
